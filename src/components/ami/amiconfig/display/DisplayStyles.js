import {
  Button,
  InputBase,
  makeStyles,
  styled,
  alpha,
} from "@material-ui/core";

export const DisplayStyles = makeStyles((theme) => ({
  gridcontainer: {
    background: "#050038",
    color: "#fff",
    textAlign: "left",
    minHeight: "80px",
  },

  gridcontainer1: {
    background: "#050038",
    color: "#fff",
    textAlign: "left",
    minHeight: "80px",
  },

  headerText: {
    fontWeight: "bold",
    fontSize: "20px",
  },
}));
